<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bill_e">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BillE">
        <activity android:name=".view.IngresarClienteActivity"></activity>
        <activity android:name=".view.IngresarProductoActivity" />
        <activity android:name=".view.CantidadProductosActivity" />
        <activity android:name=".view.RealizarVentaActivity" />
        <activity android:name=".view.SeleccionarClienteActivity" />
        <activity android:name=".view.EmpleadoActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>